schema = 5
id = "diagnostic-ap-two-client-udp-tx-stack-egress-queue-task-poll"
description = "Two-client AP UDP TX stack-selected destination burst with aggregate/BA phase evidence"
repetitions = 1
transfer = "identical-image"
tags = ["diagnostic", "wifi", "access-point", "multi-client", "udp", "tx", "txq", "stack-egress", "task-poll"]

[wifi]
image = "diagnostic-task-poll"

[wifi.workload]
kind = "access-point"
link = { phy = "ht40" }
cycles = 3
boots = 1
timeout_seconds = 180
clients = { kind = "laptop-and-openwrt" }

[wifi.workload.traffic]
kind = "udp-multi-client"
duration_seconds = 16
payload_bytes = 1472
offer = { tx_bps = 65000000 }

[wifi.workload.traffic.criteria]
minimum_tx_bps = 1
minimum_bps_per_flow = 1
maximum_flow_skew_percent = 100
