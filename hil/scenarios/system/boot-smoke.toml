schema = 5
id = "boot-smoke"
description = "Bootstrap, PSRAM mapping, PSRAM task stack, SRAM IRQ stack and runtime startup"
transfer = "identical-image"
tags = ["smoke", "psram-stack"]

[system]
kind = "boot-smoke"
