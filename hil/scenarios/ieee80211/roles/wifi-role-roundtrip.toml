schema = 5
id = "wifi-role-roundtrip"
description = "Station, idle, scan and monitor ownership roundtrip"
tags = ["qualification", "wifi", "lifecycle"]

[wifi]
image = "correctness"

[wifi.workload]
kind = "role"
link = { phy = "ht40" }
timeout_seconds = 90
operation = { kind = "roundtrip", channel = 6, dwell_seconds = 3, snapshot_length = 0 }
