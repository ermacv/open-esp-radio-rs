[package]
name = "open-esp-radio-bluetooth-hci"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Allocation-free HCI boundary for open ESP radio Bluetooth controllers"

[dependencies]
bt-hci = "=0.10.1"
embassy-sync = "0.8.0"
embedded-io = "0.7.1"

[dev-dependencies]
critical-section = { version = "1.2.0", features = ["std"] }
embassy-futures = "0.1.2"
embedded-io-async = "0.7.0"
trouble-host = { version = "=0.8.0", default-features = false, features = ["peripheral"] }

[lints]
workspace = true
