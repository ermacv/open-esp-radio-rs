schema = 5
id = "bluetooth-peripheral-wrong-key"
description = "Reject the first encryption attempt with wrong-key, verify exact security evidence, then reconnect with a valid LTK and retire cold"
repetitions = 3
tags = ["bluetooth", "peripheral", "encryption", "security-failure", "lifecycle"]

[bluetooth]
kind = "security-failure"
failure = "wrong-key"
