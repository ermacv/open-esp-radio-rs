schema = 4
id = "bluetooth-trouble-secure-gatt"
description = "Automated Numeric Comparison, denied plaintext ATT, pairing rejection, authenticated read/write/notify and RAM-bonded reconnect before and after physical Controller cold restart without SoC reset; temporary Linux bond removed on cleanup"
image = "bluetooth-secure-gatt"
isolation = "reset"
repetitions = 3
tags = ["bluetooth", "peripheral", "gatt", "trouble", "security", "automated"]

[workload]
kind = "bluetooth-secure-gatt"
