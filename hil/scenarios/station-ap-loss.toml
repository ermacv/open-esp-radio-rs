schema = 4
id = "station-ap-loss"
description = "Station detects controlled AP loss and reconnects"
image = "correctness"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["diagnostic", "psram-stack", "station", "lifecycle"]

[link]
phy = "ht40"

[workload]
kind = "station-ap-loss"
timeout_seconds = 120

[criteria]
