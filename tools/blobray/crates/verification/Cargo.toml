[package]
name = "blobray-verification"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Concrete observation comparison without execution authority"

[dependencies]
blobray-domain = { path = "../domain" }

[lints]
workspace = true

[package.metadata.open-radio]
scope = "development"
layer = "tool"
platform = "host"
