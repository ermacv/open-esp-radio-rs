schema = 4
transfer = "identical-image"
id = "bluetooth-trouble-secure-gatt-timing"
description = "Secure lifecycle observer comparison: IRQ watermark scans only at workload boundaries; not a substitute for every-snapshot memory stress"
image = "bluetooth-secure-gatt"
isolation = "reset"
repetitions = 3
tags = ["bluetooth", "peripheral", "gatt", "trouble", "security", "diagnostic", "timing"]

[workload]
kind = "bluetooth-secure-gatt-timing"
