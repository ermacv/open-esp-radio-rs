schema = 3
id = "tcp-tx-ht40-split"
description = "Split-core HT40 sustained TCP station-to-host transmission"
repetitions = 3
image = "performance"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["qualification", "tcp", "station", "ht40", "split-core"]

[link]
phy = "ht40"

[workload]
kind = "tcp"
direction = "tx"
duration_seconds = 16
tx_rate_bps = 120000000
chunk_bytes = 32768

[criteria]
minimum_tx_bps = 65000000
