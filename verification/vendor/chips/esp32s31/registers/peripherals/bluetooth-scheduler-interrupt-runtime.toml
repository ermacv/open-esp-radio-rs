schema = 2

[[peripherals]]
name = "BLUETOOTH_SCHEDULER_INTERRUPT_RUNTIME"
description = """
Two scheduler words transferred with BLUETOOTH_INTERRUPT_BANK to the primary
hard handler. The complete source-124 suffix can read SCHEDULER_STATE at two
distinct temporal points and can write a complete zero image to
SCHEDULER_REFERENCE. Keeping this sparse physical view outside the task-side
controller partition prevents either owner from manufacturing an MMIO alias.
"""
baseAddress = 0x20101000

[[peripherals.registers]]
[peripherals.registers.register]
name = "SCHEDULER_REFERENCE"
description = "The source-124 handler can publish the complete zero image when bank-one source 3 observes an idle scheduler. Other scheduler users compare bits 0..19 or publish complete images; upper encoding remains unknown."
addressOffset = 0x78
size = 32
access = "read-write"
[[peripherals.registers.register.fields]]
name = "VALUE_LOW_20"
bitOffset = 0
bitWidth = 20
[[peripherals.registers.register.fields]]
name = "OPAQUE_HIGH_12"
bitOffset = 20
bitWidth = 12

[[peripherals.registers]]
[peripherals.registers.register]
name = "SCHEDULER_STATE"
description = "The source-124 handler reads this complete shared scheduler state at a bank-one reference gate and again before publishing deferred work. Command transactions also wait while bit 31 is set; other complete users consume or publish the low 20 and low 30 bits."
addressOffset = 0x7C
size = 32
access = "read-write"
[[peripherals.registers.register.fields]]
name = "VALUE_LOW_20"
bitOffset = 0
bitWidth = 20
[[peripherals.registers.register.fields]]
name = "OPAQUE_STATE_20_29"
bitOffset = 20
bitWidth = 10
[[peripherals.registers.register.fields]]
name = "OPAQUE_STATE_30"
bitOffset = 30
bitWidth = 1
[[peripherals.registers.register.fields]]
name = "BUSY"
bitOffset = 31
bitWidth = 1

[[review]]
entity = "BLUETOOTH_SCHEDULER_INTERRUPT_RUNTIME.SCHEDULER_REFERENCE"
sources = ["BLOB_LIBBTDM_SCHEDULER_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "partial"

[[review]]
entity = "BLUETOOTH_SCHEDULER_INTERRUPT_RUNTIME.SCHEDULER_REFERENCE.VALUE_LOW_20"
sources = ["BLOB_LIBBTDM_SCHEDULER_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "complete"

[[review]]
entity = "BLUETOOTH_SCHEDULER_INTERRUPT_RUNTIME.SCHEDULER_REFERENCE.OPAQUE_HIGH_12"
sources = ["BLOB_LIBBTDM_SCHEDULER_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "partial"

[[review]]
entity = "BLUETOOTH_SCHEDULER_INTERRUPT_RUNTIME.SCHEDULER_STATE"
sources = ["BLOB_LIBBTDM_SCHEDULER_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "partial"

[[review]]
entity = "BLUETOOTH_SCHEDULER_INTERRUPT_RUNTIME.SCHEDULER_STATE.VALUE_LOW_20"
sources = ["BLOB_LIBBTDM_SCHEDULER_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "complete"

[[review]]
entity = "BLUETOOTH_SCHEDULER_INTERRUPT_RUNTIME.SCHEDULER_STATE.OPAQUE_STATE_20_29"
sources = ["BLOB_LIBBTDM_SCHEDULER_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "partial"

[[review]]
entity = "BLUETOOTH_SCHEDULER_INTERRUPT_RUNTIME.SCHEDULER_STATE.OPAQUE_STATE_30"
sources = ["BLOB_LIBBTDM_SCHEDULER_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "partial"

[[review]]
entity = "BLUETOOTH_SCHEDULER_INTERRUPT_RUNTIME.SCHEDULER_STATE.BUSY"
sources = ["BLOB_LIBBTDM_SCHEDULER_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "complete"
