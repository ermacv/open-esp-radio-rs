schema = 3
id = "tcp-tx"
description = "Sustained TCP station-to-host transmission"
image = "correctness"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["qualification", "tcp", "station"]

[link]
phy = "he20"

[workload]
kind = "tcp"
direction = "tx"
duration_seconds = 16
tx_rate_bps = 70000000
chunk_bytes = 32768

[criteria]
minimum_tx_bps = 50000000
require_no_beacon_loss = true
