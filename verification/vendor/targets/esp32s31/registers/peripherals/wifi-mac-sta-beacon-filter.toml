schema = 2

[[peripherals]]
name = "WIFI_MAC_STA_BEACON_FILTER"
description = "Station beacon-filter control word recovered from the complete enable and disable leaves. The three low bits are toggled together; individual hardware meanings remain unknown."
baseAddress = 0x201042B4

[[peripherals.registers]]

[peripherals.registers.register]
name = "CONTROL"
description = "hal_enable_sta_beacon_filter sets bits 2:0 after enabling the matching interrupt; hal_disable_sta_beacon_filter clears them before disabling that interrupt."
addressOffset = 0x0
size = 32
access = "read-write"

[[peripherals.registers.register.fields]]
name = "ENABLES_UNKNOWN"
bitOffset = 0
bitWidth = 3

[peripherals.registers.register.fields.writeConstraint.range]
minimum = 0
maximum = 7

[[review]]
entity = "WIFI_MAC_STA_BEACON_FILTER"
sources = ["BLOB_LIBPP_HAL_RUNTIME_MMIO_LEAVES"]
confidence = "instruction-exact-semantics-unknown"

[[review]]
entity = "WIFI_MAC_STA_BEACON_FILTER.CONTROL"
sources = ["BLOB_LIBPP_HAL_RUNTIME_MMIO_LEAVES"]
confidence = "instruction-exact-semantics-unknown"
