schema = 2

[[peripherals]]
name = "PHY_COLD_DEADLINE_ORACLE"
description = """
Free-running word sampled by the complete rev0 ROM SDM-stability
        deadline. The instruction body proves its address and unsigned
        wraparound consumer, but not its hardware clock source."""
baseAddress = 0x2010D800

[[peripherals.registers]]

[peripherals.registers.register]
name = "DEADLINE_COUNTER_UNKNOWN"
description = "Complete phy_wait_i2c_sdm_stable samples this full word before and after each independently completed PHY-I2C read."
addressOffset = 0x0
size = 32
access = "read-only"

[[peripherals.registers.register.fields]]
name = "VALUE"
description = "Full-width wrapping deadline value."
bitOffset = 0
bitWidth = 32

[[review]]
entity = "PHY_COLD_DEADLINE_ORACLE"
sources = ["ROM_REV0_PHY_WAIT_I2C_SDM_STABLE"]
provenance = "observed"
accuracy = "exact"
completeness = "unknown"

[[review]]
entity = "PHY_COLD_DEADLINE_ORACLE.DEADLINE_COUNTER_UNKNOWN"
sources = ["ROM_REV0_PHY_WAIT_I2C_SDM_STABLE"]
provenance = "observed"
accuracy = "exact"
completeness = "unknown"

[[review]]
entity = "PHY_COLD_DEADLINE_ORACLE.DEADLINE_COUNTER_UNKNOWN.VALUE"
sources = ["ROM_REV0_PHY_WAIT_I2C_SDM_STABLE"]
provenance = "observed"
accuracy = "exact"
completeness = "complete"
