[package]
name = "open-esp-radio-embassy-net"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Bounded, owned Ethernet frame boundary between open ESP radio tasks and embassy-net"

[features]
default = []
# Exposes the exact pre-publication edge only to diagnostic compositions.
diagnostics = []

[dependencies]
embassy-net-driver = { git = "https://github.com/embassy-rs/embassy", rev = "e7a576f22393edaf9070f29ee5f4f07f5f4d8782" }
embassy-sync = "0.8.0"
open-esp-radio-dma = { path = "../../common/dma" }

[dev-dependencies]
embassy-executor = { version = "0.10.0", features = ["executor-thread", "platform-std"] }
embassy-net = { git = "https://github.com/embassy-rs/embassy", rev = "e7a576f22393edaf9070f29ee5f4f07f5f4d8782", features = ["icmp", "medium-ethernet", "proto-ipv4", "std"] }
embassy-time = { version = "0.5.0", features = ["std"] }

[lints]
workspace = true
