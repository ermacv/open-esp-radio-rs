schema = 5
id = "bluetooth-peripheral-missing-key"
description = "Reject the first encryption attempt with missing-key, verify exact security evidence, then reconnect with a valid LTK and retire cold"
repetitions = 3
tags = ["bluetooth", "peripheral", "encryption", "security-failure", "lifecycle"]

[bluetooth]
kind = "security-failure"
failure = "missing-key"
