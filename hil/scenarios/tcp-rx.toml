schema = 4
id = "tcp-rx"
description = "Sustained TCP host-to-station receive"
image = "correctness"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["qualification", "tcp", "station"]

[link]
phy = "he20"

[workload]
kind = "tcp"
direction = "rx"
duration_seconds = 16
rx_rate_bps = 80000000
chunk_bytes = 32768

[criteria]
minimum_rx_bps = 60000000
require_no_beacon_loss = true
