schema = 5
id = "wifi-scan"
description = "Standalone full-band scan with target-side dwell timing"
tags = ["qualification", "wifi", "scanner"]

[wifi]
image = "correctness"

[wifi.workload]
kind = "role"
link = { phy = "ht40" }
timeout_seconds = 90
operation = { kind = "scan" }
