[package]
name = "open-esp-radio-esp32s31-embassy-wifi"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.1"
license = "MIT OR Apache-2.0"
repository = "https://github.com/ermacv/open-esp-radio-rs"
description = "Product-level ESP32-S31 Wi-Fi composition for Embassy and esp-hal"

[dependencies]
embassy-executor = { version = "0.10.0", default-features = false }
embassy-futures = "0.1.2"
embassy-sync = "0.8.0"
embassy-time = "0.5.1"
esp-hal = { git = "https://github.com/ermacv/esp-hal", branch = "esp32s31-async-platform", default-features = false, features = ["esp32s31", "rt", "unstable"] }
log = { version = "0.4.29", optional = true }
open-esp-radio = { path = "../../../radio", default-features = false, features = ["esp32s31-wifi-embassy"] }
open-esp-radio-embassy-net = { path = "../../../adapters/embassy-net" }
open-esp-radio-esp32s31-wifi-embassy = { path = "../../../adapters/embassy/esp32s31-wifi" }
open-esp-radio-esp32s31-wifi-esp-hal = { path = "../../../adapters/esp-hal/esp32s31-wifi", features = ["esp32s31"] }
open-esp-radio-wifi-embassy = { path = "../../../adapters/embassy/wifi" }
static_cell = "2.1.1"

[features]
default = []
high-throughput = ["open-esp-radio-esp32s31-wifi-embassy/high-throughput"]
qualification = [
    "dep:log",
    "high-throughput",
    "open-esp-radio-embassy-net/rx-delivery-observation",
    "open-esp-radio-esp32s31-wifi-embassy/rx-delivery-observation",
]

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[workspace]
resolver = "3"

[patch."https://github.com/esp-rs/esp-pacs"]
esp32s31 = { git = "https://github.com/ermacv/esp-pacs", rev = "d0fb94ef3" }
