[package]
name = "oer-esp32s31-platform-layout"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Staged-boot address map, runtime placement profiles and stage-two image contract"
readme = "README.md"

[features]
# Emit the layout to linker scripts from a Cargo build script (requires std).
build = []

[lints]
workspace = true

[package.metadata.docs.rs]
features = ["build"]

[package.metadata.open-radio]
scope = "development"
layer = "platform"
platform = "chip"
chip = "esp32s31"
