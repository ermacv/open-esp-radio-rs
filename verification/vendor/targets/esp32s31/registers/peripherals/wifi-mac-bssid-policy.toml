schema = 2

[[peripherals]]
name = "WIFI_MAC_BSSID_POLICY"
description = "Per-interface BSSID address and policy words used by scan and associated-STA receive filtering."
baseAddress = 0x20104000

[[peripherals.registers]]

[peripherals.registers.register]
dim = 4
dimIncrement = 0x8
dimIndex = "0,1,2,3"
name = "BSSID_LOW%s"
description = "BSSID bytes 0..3 for interfaces 0..3; hal_mac_set_bssid publishes a complete full-word store."
addressOffset = 0x0
size = 32
access = "read-write"

[[peripherals.registers.register.fields]]
name = "BYTES_0_3"
bitOffset = 0
bitWidth = 32

[peripherals.registers.register.fields.writeConstraint.range]
minimum = 0x0
maximum = 0xFFFFFFFF

[[peripherals.registers]]

[peripherals.registers.register]
dim = 4
dimIncrement = 0x8
dimIndex = "0,1,2,3"
name = "BSSID_HIGH%s"
description = "Canonical high BSSID, association identity and interface policy word for interfaces 0..3. Complete dbg_read_rx_misc independently decodes every field; the HE association leaf updates the MMSS/AID fields of interface zero rather than defining a second register identity."
addressOffset = 0x4
size = 32
access = "read-write"

[[peripherals.registers.register.fields]]
name = "BSSID_HIGH"
bitOffset = 0
bitWidth = 16

[peripherals.registers.register.fields.writeConstraint.range]
minimum = 0x0
maximum = 0xFFFF

[[peripherals.registers.register.fields]]
name = "ASSOCIATION_ID"
description = "Eleven-bit AID printed by dbg_read_rx_misc."
bitOffset = 16
bitWidth = 11

[peripherals.registers.register.fields.writeConstraint.range]
minimum = 0x0
maximum = 0x7FF

[[peripherals.registers.register.fields]]
name = "MINIMUM_MPDU_START_SPACING"
description = "Three-bit MMSS value printed by dbg_read_rx_misc."
bitOffset = 27
bitWidth = 3

[peripherals.registers.register.fields.writeConstraint.range]
minimum = 0x0
maximum = 0x7

[[peripherals.registers.register.fields]]
name = "INTERFACE_IS_SOFT_AP"
description = "Cleared for interface zero by the associated-STA policy-five transaction."
bitOffset = 30
bitWidth = 1

[[peripherals.registers.register.fields]]
name = "ADDRESS_CHECK_ENABLE"
description = "MACBSSID_EN: hal_mac_set_bssid clears this bit before changing the address and sets it after publication; the reachable associated-STA policy branch also sets it."
bitOffset = 31
bitWidth = 1

[[review]]
entity = "WIFI_MAC_BSSID_POLICY"
sources = [
    "BLOB_LIBPP_HAL_MAC_SET_BSSID",
    "BLOB_LIBPP_RX_POLICY",
    "BLOB_LIBNET80211_WIFI_RX_POLICY",
]
confidence = "instruction-exact-hil-qualified"

[[review]]
entity = "WIFI_MAC_BSSID_POLICY.BSSID_LOW%s"
sources = ["BLOB_LIBPP_HAL_MAC_SET_BSSID"]
confidence = "instruction-exact"

[[review]]
entity = "WIFI_MAC_BSSID_POLICY.BSSID_HIGH%s"
sources = [
    "BLOB_LIBPP_HAL_MAC_SET_BSSID",
    "BLOB_LIBPP_RX_POLICY",
    "BLOB_LIBPP_HAL_RX_MISC_DEBUG",
    "BLOB_LIBPP_HAL_HE_SET_MMSS_AND_AID",
    "MIGRATION_HE20_ASSOCIATION",
]
confidence = "instruction-exact"

[[review]]
entity = "WIFI_MAC_BSSID_POLICY.BSSID_HIGH%s.ASSOCIATION_ID"
sources = ["BLOB_LIBPP_HAL_RX_MISC_DEBUG"]
confidence = "instruction-exact"

[[review]]
entity = "WIFI_MAC_BSSID_POLICY.BSSID_HIGH%s.MINIMUM_MPDU_START_SPACING"
sources = ["BLOB_LIBPP_HAL_RX_MISC_DEBUG"]
confidence = "instruction-exact"

[[review]]
entity = "WIFI_MAC_BSSID_POLICY.BSSID_HIGH%s.INTERFACE_IS_SOFT_AP"
sources = [
    "BLOB_LIBPP_HAL_RX_MISC_DEBUG",
    "BLOB_LIBPP_RX_POLICY",
]
confidence = "instruction-exact"

[[review]]
entity = "WIFI_MAC_BSSID_POLICY.BSSID_HIGH%s.ADDRESS_CHECK_ENABLE"
sources = [
    "BLOB_LIBPP_HAL_MAC_SET_BSSID",
    "BLOB_LIBPP_RX_POLICY",
    "BLOB_LIBNET80211_WIFI_RX_POLICY",
    "BLOB_LIBPP_HAL_RX_MISC_DEBUG",
]
confidence = "instruction-exact-partial"
