schema = 4
id = "diagnostic-ap-balanced-airtime"
description = "Two AP downlink streams offered 130 Mbit/s each with task-poll and optional per-peer service-budget accounting"
repetitions = 1
image = "diagnostic-task-poll"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["diagnostic", "access-point", "multi-client", "ht40", "airtime"]

[link]
phy = "ht40"

[workload]
kind = "access-point"
cycles = 1
boots = 1
timeout_seconds = 150
client = "laptop"

[workload.traffic]
kind = "udp-multi-client"
direction = "tx"
duration_seconds = 12
payload_bytes = 1472
tx_rate_bps_per_flow = 130000000

[criteria]
exact_delivery = false
minimum_bps_per_flow = 1
minimum_concurrent_ap_clients = 2
minimum_tx_bps = 1
