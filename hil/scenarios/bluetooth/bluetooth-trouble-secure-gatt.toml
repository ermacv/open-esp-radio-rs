schema = 4
id = "bluetooth-trouble-secure-gatt"
description = "Automated Numeric Comparison, protected traffic and RAM-bonded reconnect across cold restart with a withheld Reset reader; injected bond-load failure requires terminal cold close without restart; temporary Linux bond removed on cleanup"
image = "bluetooth-secure-gatt"
isolation = "reset"
repetitions = 3
tags = ["bluetooth", "peripheral", "gatt", "trouble", "security", "automated"]

[workload]
kind = "bluetooth-secure-gatt"
