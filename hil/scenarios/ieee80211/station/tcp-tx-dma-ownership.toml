schema = 5
id = "tcp-tx-dma-ownership"
description = "TCP transmit with receive-side DMA lease timing for peer traffic"
repetitions = 3
transfer = "identical-image"
tags = ["diagnostic", "tcp", "station", "memory"]

[wifi]
image = "diagnostic-rx-ownership"

[wifi.workload]
kind = "station-tcp"
link = { phy = "ht40" }
duration_seconds = 16
chunk_bytes = 32768
offer = { tx_bps = 120000000 }

[wifi.workload.criteria]
minimum_tx_bps = 65000000
