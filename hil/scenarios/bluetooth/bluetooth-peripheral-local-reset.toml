schema = 5
id = "bluetooth-peripheral-local-reset"
description = "Two real LE peripheral connections with exact Feature and Version Exchange, ACL backpressure, Connection and Channel Map Updates, a second ACL exchange, target HCI Reset, peer supervision timeout and reinitialized advertising"
repetitions = 3
tags = ["bluetooth", "peripheral", "connection", "feature-exchange", "version-exchange", "connection-update", "channel-map-update", "reset", "acl", "recovery"]

[bluetooth]
kind = "peripheral"
boots = 1
connections = 2
hold_millis = 100
termination = "target-reset"
