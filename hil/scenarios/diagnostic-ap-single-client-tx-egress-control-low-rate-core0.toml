schema = 4
id = "diagnostic-ap-single-client-tx-egress-control-low-rate-core0"
description = "Low-rate AP TX proving sparse affine egress-demand progress without saturation feedback"
repetitions = 1
image = "diagnostic-core0-rx-coarse"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["diagnostic", "wifi", "access-point", "network", "single-client", "ht40", "split-core", "tx", "core0", "egress-control"]

[link]
phy = "ht40"

[workload]
kind = "access-point"
cycles = 2
boots = 1
timeout_seconds = 90
client = "open-wrt"

[workload.traffic]
kind = "udp"
direction = "tx"
duration_seconds = 8
tx_rate_bps = 1000000
payload_bytes = 1472

[criteria]
exact_delivery = false
minimum_tx_bps = 1
minimum_concurrent_ap_clients = 1
