schema = 4
id = "bluetooth-trouble-gatt"
description = "Shared standalone Trouble application: discover, read/write and gracefully disconnect/reconnect three times on one Host/Controller epoch; plaintext, not secure GATT qualification"
image = "bluetooth-gatt"
isolation = "reset"
repetitions = 3
tags = ["bluetooth", "peripheral", "gatt", "trouble", "lifecycle"]

[workload]
kind = "bluetooth-gatt"
