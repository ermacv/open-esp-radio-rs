# ESP32-S31 vendor validation harness data

Public ABI and lifecycle fixture data for the ESP32-S31 validation harness:
external callback tables, mutable pointer-cell entry states and ROM function
table bindings. It depends only on `open-radio-vendor-validator-core` and does
not load or authenticate vendor artifacts.
