schema = 4
id = "diagnostic-station-phy-rxcal-hot-sram-delivery-rx"
description = "Common-calibration timing with RX-gain state decisions in SRAM and the same short-settle policy"
repetitions = 1
image = "diagnostic-rx-delivery-phy-hot-sram"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["diagnostic", "station", "pause", "udp", "ht40"]

[link]
phy = "ht40"

[workload]
kind = "udp"
direction = "rx"
duration_seconds = 12
rx_rate_bps = 65000000
payload_bytes = 1200
station_pause = "common-calibration"

[criteria]
exact_delivery = false
minimum_rx_bps = 10000000
require_no_beacon_loss = true
