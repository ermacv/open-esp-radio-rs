schema = 3
id = "access-point-single-client-ceiling-tx"
description = "Single-client AP transmit ceiling characterization"
repetitions = 1
image = "qualification"
isolation = "reset"
tags = ["characterization", "wifi", "access-point", "network", "single-client", "ceiling", "tx"]

[workload]
kind = "access-point"
cycles = 2
boots = 1
timeout_seconds = 120

[workload.traffic]
kind = "udp"
direction = "tx"
duration_seconds = 16
tx_rate_bps = 40000000
payload_bytes = 1400

[criteria]
exact_delivery = false
minimum_concurrent_ap_clients = 1
