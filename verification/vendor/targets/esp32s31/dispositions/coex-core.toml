schema = 1
# Reviewed non-RTOS coexistence core leaves. Scheduler and synchronization
# entry points remain outside this manifest until their external-call models
# are executable.
default-disposition = "not-yet-ported"
default-protocol = "coex"

[[functions]]
source = "coex"
symbol = "coex_core_pti_get"
disposition = "direct"
protocol = "coex"
rust-component = "open_esp_radio_esp32s31_coex::CoexPtiTable::pti"
