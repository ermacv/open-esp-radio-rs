schema = 3
id = "udp-tx-emission-736-no-checksum-diagnostic"
description = "Fixed-rate 736-byte UDP TX checksum-omission emission-cost diagnostic"
repetitions = 2
image = "diagnostic-core0-rx-coarse"
isolation = "reset"
data_plane = "split-radio-network"
tx_udp_checksum = "omit-ipv4-diagnostic"
tags = ["diagnostic", "udp", "station", "tx", "ht40", "split-core", "core0", "coarse-cycles", "tx-emission-sweep"]

[link]
phy = "ht40"
guard_interval = "any"

[workload]
kind = "udp"
direction = "tx"
duration_seconds = 12
tx_rate_bps = 29440000
payload_bytes = 736

[criteria]
exact_delivery = false
require_no_beacon_loss = false
minimum_tx_bps = 25000000
maximum_idle_channel_utilization_255 = 64
