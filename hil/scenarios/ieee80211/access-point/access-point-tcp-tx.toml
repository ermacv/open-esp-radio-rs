schema = 5
id = "access-point-tcp-tx"
description = "Two AP ownership epochs with exact outbound TCP delivery"
transfer = "identical-image"
tags = ["qualification", "wifi", "access-point", "network", "tcp", "tx"]

[wifi]
image = "correctness"

[wifi.workload]
kind = "access-point"
cycles = 2
boots = 1
timeout_seconds = 90

[wifi.workload.traffic]
kind = "tcp"
duration_seconds = 10
chunk_bytes = 8192
offer = { tx_bps = 5000000 }

[wifi.workload.traffic.criteria]
minimum_tx_bps = 4000000
