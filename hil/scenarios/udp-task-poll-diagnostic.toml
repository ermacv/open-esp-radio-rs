schema = 2
id = "udp-task-poll-diagnostic"
description = "Embassy poll-residence diagnostic under UDP RX load"
image = "diagnostic-task-poll"
isolation = "reset"
tags = ["diagnostic", "udp", "station"]

[workload]
kind = "udp"
direction = "rx"
duration_seconds = 16
rx_rate_bps = 80000000
payload_bytes = 1200
phy = "he20"

[criteria]
exact_delivery = true
