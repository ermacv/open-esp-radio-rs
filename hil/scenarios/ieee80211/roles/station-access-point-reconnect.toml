schema = 5
id = "station-access-point-reconnect"
description = "Upstream beacon loss tears down the complete STA+AP owner and a restored fixture permits one explicit fresh pair"
tags = ["wifi", "sta-ap", "reconnect", "beacon-loss", "psram-stack"]

[wifi]
image = "correctness"

[wifi.workload]
kind = "station-access-point-reconnect"
link = { phy = "ht40" }
timeout_seconds = 120
