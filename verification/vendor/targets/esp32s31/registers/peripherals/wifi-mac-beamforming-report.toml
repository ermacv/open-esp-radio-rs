schema = 2

[[peripherals]]
name = "WIFI_MAC_BEAMFORMING_REPORT"
description = "Complete esp_test_get_bfr_avgsnr reads the low byte at 0x20105f94 as stream-zero average-SNR code. Code 0x7f is a special saturated/invalid value rendered with a greater-than-or-equal prefix; every other code is converted to dB as (code + 88) / 4."
baseAddress = 0x20105F94

[[peripherals.registers]]

[peripherals.registers.register]
name = "AVERAGE_SNR"
description = "Beamforming-report average SNR observation used by complete esp_test_get_bfr_avgsnr."
addressOffset = 0x0
size = 32
access = "read-only"

[[peripherals.registers.register.fields]]
name = "STREAM_0_CODE"
description = "Unsigned raw code. Values other than 0x7f convert to quarter-dB units after adding 88."
bitOffset = 0
bitWidth = 8

[[peripherals.registers.register.fields]]
name = "UNKNOWN"
bitOffset = 8
bitWidth = 24

[[review]]
entity = "WIFI_MAC_BEAMFORMING_REPORT"
sources = ["BLOB_LIBPP_BFR_AVERAGE_SNR"]
confidence = "instruction-exact"
