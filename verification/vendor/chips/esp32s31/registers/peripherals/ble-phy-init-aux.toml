schema = 2

[[peripherals]]
name = "BLE_PHY_INIT_TOGGLE"
description = """
Narrow owner for the otherwise unbounded word at 0x20101FF0. Complete
ESP32-S31 BLE PHY initialization writes zero and then one before touching any
other radio register. The same ordered operation appears at the same block
offset in the named ESP32-C61 counterpart, but neither artifact proves that
this word belongs to the adjacent BTMAC dump range. No wider peripheral extent
or reset/enable interpretation is claimed.
"""
baseAddress = 0x20101FF0

[[peripherals.registers]]
[peripherals.registers.register]
name = "INIT_TOGGLE"
description = "Complete ordered images 0 then 1 at BLE PHY register-initialization entry."
addressOffset = 0x0
size = 32
access = "write-only"
[[peripherals.registers.register.fields]]
name = "IMAGE"
bitOffset = 0
bitWidth = 32

[[review]]
entity = "BLE_PHY_INIT_TOGGLE"
sources = ["BLOB_LIBBLE_PHY_INIT_REGISTERS"]
provenance = "derived"
accuracy = "exact"
completeness = "complete"
[[review]]
entity = "BLE_PHY_INIT_TOGGLE.INIT_TOGGLE"
sources = ["BLOB_LIBBLE_PHY_INIT_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "complete"
[[review]]
entity = "BLE_PHY_INIT_TOGGLE.INIT_TOGGLE.IMAGE"
sources = ["BLOB_LIBBLE_PHY_INIT_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "complete"

[[peripherals]]
name = "BLE_PHY_INIT_PHASE"
description = """
Narrow owner for the otherwise isolated word at 0x2010891C. Complete
ESP32-S31 BLE PHY initialization writes phase image 0x20 before initializing
the BLE hardware accelerator and image 0x40 after the surrounding BTMAC and
accelerator sequence. The register's physical block and the meanings of the
two phases remain unproved, so no neighboring 0x201088xx words are absorbed.
"""
baseAddress = 0x2010891C

[[peripherals.registers]]
[peripherals.registers.register]
name = "INIT_PHASE"
description = "Complete ordered images 0x20 then 0x40 around BLE accelerator initialization."
addressOffset = 0x0
size = 32
access = "write-only"
[[peripherals.registers.register.fields]]
name = "IMAGE"
bitOffset = 0
bitWidth = 32

[[review]]
entity = "BLE_PHY_INIT_PHASE"
sources = ["BLOB_LIBBLE_PHY_INIT_REGISTERS"]
provenance = "derived"
accuracy = "exact"
completeness = "complete"
[[review]]
entity = "BLE_PHY_INIT_PHASE.INIT_PHASE"
sources = ["BLOB_LIBBLE_PHY_INIT_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "complete"
[[review]]
entity = "BLE_PHY_INIT_PHASE.INIT_PHASE.IMAGE"
sources = ["BLOB_LIBBLE_PHY_INIT_REGISTERS"]
provenance = "observed"
accuracy = "exact"
completeness = "complete"
