schema = 2

[[peripherals]]
name = "BLE_CONTROLLER_POINTER_PUBLICATION"
description = """
Narrow positional owner for one controller word written by six ble_hw.c
paths. The software source context is a useful ownership hint, while neither
the destination's hardware action nor the dynamic value's inner meaning is
claimed.
"""
baseAddress = 0x20101874

[[peripherals.registers]]
[peripherals.registers.register]
name = "VALUE"
description = "Observed complete publications contain a dynamic value in bits 0..19 and zero in bits 20..31."
addressOffset = 0x0
size = 32
access = "write-only"

[[peripherals.registers.register.fields]]
name = "IMAGE_LOW_20"
description = "Dynamic low-twenty-bit image; its pointer or index semantics are not established."
bitOffset = 0
bitWidth = 20

[[review]]
entity = "BLE_CONTROLLER_POINTER_PUBLICATION"
sources = ["BLOB_LIBBLE_CONTROLLER_POINTER_PUBLICATION"]
provenance = "derived"
accuracy = "exact"
completeness = "partial"

[[review]]
entity = "BLE_CONTROLLER_POINTER_PUBLICATION.VALUE"
sources = ["BLOB_LIBBLE_CONTROLLER_POINTER_PUBLICATION"]
provenance = "observed"
accuracy = "exact"
completeness = "partial"

[[review]]
entity = "BLE_CONTROLLER_POINTER_PUBLICATION.VALUE.IMAGE_LOW_20"
sources = ["BLOB_LIBBLE_CONTROLLER_POINTER_PUBLICATION"]
provenance = "observed"
accuracy = "exact"
completeness = "complete"
