schema = 5
id = "memory-copy-benchmark"
description = "Bounded CPU, blocking GDMA and async GDMA copies from SRAM/PSRAM into SRAM; correctness and measured counter scopes"
transfer = "identical-image"
tags = ["diagnostic", "memory", "copy", "gdma"]

[system]
kind = "memory-benchmark"
boots = 2
iterations = 32
sizes = [64, 256, 512, 1514, 4096]
batch_sizes = [1]
