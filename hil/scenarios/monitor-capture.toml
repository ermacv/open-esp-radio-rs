schema = 3
id = "monitor-capture"
description = "Finite typed monitor capture exported as PCAPNG"
image = "correctness"
isolation = "reset"
tags = ["qualification", "wifi", "monitor"]

[link]
phy = "ht40"

[workload]
kind = "monitor-capture"
timeout_seconds = 90
duration_seconds = 3
snapshot_length = 0

[criteria]
