schema = 5
id = "bluetooth-peripheral-encrypted-phy-maintenance"
description = "Require encrypted ACL progress before and after automatic PHY maintenance on the same connection, repeat after peer Reset, then retire cold"
repetitions = 3
tags = ["bluetooth", "peripheral", "acl", "encryption", "phy-maintenance", "lifecycle"]

[bluetooth]
kind = "encrypted-acl"
exercise = "active-maintenance"
