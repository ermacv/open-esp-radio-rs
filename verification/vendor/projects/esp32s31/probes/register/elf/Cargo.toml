[package]
name = "oer-esp32s31-probe-register-elf"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
build = "build.rs"
description = "Linked ESP32-S31 PAC/HAL register probe image"

[dependencies]
oer-esp32s31-probe-register = { path = "../library" }

[lints.rust]
unexpected_cfgs = "allow"

[package.metadata.open-radio]
scope = "development"
layer = "verification"
platform = "chip"
chip = "esp32s31"

[build-dependencies]
oer-probe-codegen = { path = "../../../../../harness/codegen" }
