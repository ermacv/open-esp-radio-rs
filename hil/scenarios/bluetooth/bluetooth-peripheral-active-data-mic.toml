schema = 5
id = "bluetooth-peripheral-active-data-mic"
description = "Corrupt one received active-data MIC before production authentication; require zero Host delivery, MIC-failure retirement, encrypted recovery and cold release; not over-air corruption"
repetitions = 3
tags = ["bluetooth", "peripheral", "encryption", "security-failure", "rx-fault-injection", "lifecycle"]

[bluetooth]
kind = "security-failure"
failure = "active-data-mic"
