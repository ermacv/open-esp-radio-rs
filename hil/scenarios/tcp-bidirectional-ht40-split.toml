schema = 3
id = "tcp-bidirectional-ht40-split"
description = "Split-core HT40 simultaneous TCP receive and transmit"
repetitions = 3
image = "qualification"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["qualification", "tcp", "station", "full-duplex", "ht40", "split-core"]

[link]
phy = "ht40"

[workload]
kind = "tcp"
direction = "bidirectional"
duration_seconds = 16
rx_rate_bps = 30000000
tx_rate_bps = 30000000
chunk_bytes = 32768

[criteria]
minimum_rx_bps = 25000000
minimum_tx_bps = 25000000
require_no_beacon_loss = true
