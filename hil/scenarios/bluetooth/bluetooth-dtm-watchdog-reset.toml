schema = 4
id = "bluetooth-dtm-watchdog-reset"
description = "Diagnostic MWDT1 reset during active DTM TX/RX; peer checks RF silence before any new DUT HCI command"
image = "bluetooth-watchdog-reset"
isolation = "reset"
repetitions = 3
tags = ["bluetooth", "dtm", "watchdog", "fault-injection"]

[workload]
kind = "bluetooth-watchdog-reset"
