schema = 4
id = "bluetooth-peripheral-local-disconnect"
description = "Two real LE peripheral connections with exact Feature and Version Exchange, ACL backpressure, Connection and Channel Map Updates, a second ACL exchange, target Host Disconnect, peer confirmation and advertising recovery"
image = "bluetooth-dtm"
isolation = "reset"
repetitions = 3
tags = ["bluetooth", "peripheral", "connection", "feature-exchange", "version-exchange", "connection-update", "channel-map-update", "disconnect", "acl", "recovery"]

[workload]
kind = "bluetooth-peripheral"
boots = 1
connections = 2
hold_millis = 100
termination = "target-disconnect"
