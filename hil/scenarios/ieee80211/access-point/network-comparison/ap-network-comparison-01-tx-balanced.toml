schema = 4
id = "ap-network-comparison-01-tx-balanced"
description = "Two physical stations with ESP as AP: tx-balanced"
repetitions = 1
image = "diagnostic-task-residence"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["diagnostic", "access-point", "multi-client", "ht40", "ap-network-comparison"]

[link]
phy = "ht40"

[workload]
kind = "access-point"
cycles = 1
boots = 1
timeout_seconds = 150
client = "laptop"

[workload.traffic]
kind = "udp-multi-client"
direction = "tx"
duration_seconds = 12
payload_bytes = 1472
tx_rate_bps_per_flow = 65000000

[criteria]
exact_delivery = false
minimum_bps_per_flow = 1
minimum_concurrent_ap_clients = 2
minimum_tx_bps = 1
