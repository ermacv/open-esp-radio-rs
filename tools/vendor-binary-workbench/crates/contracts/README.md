# Vendor contracts

Architecture- and platform-neutral immutable contract types shared by
analysis backends and target harnesses. This crate intentionally has no
dependencies and contains no instruction semantics, chip identities, artifact
paths or input-authentication policy.
