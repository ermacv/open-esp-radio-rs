schema = 5
id = "wifi-phy-watchdog"
description = "Real connected PHY faults; autonomous reset, new-boot bidirectional UDP, rejected admission without reset"
repetitions = 3
transfer = "identical-image"
tags = ["wifi", "phy", "watchdog", "fault-injection"]

[wifi]
image = "diagnostic-phy-fault"

[wifi.workload]
kind = "phy-watchdog"
link = { phy = "ht40" }
