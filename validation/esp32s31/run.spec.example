# Copy this file outside the repository or keep the copy untracked.
# Paths are resolved relative to the copied run spec.
schema 1
input source-artifact:rom /path/to/esp32s31_rev0_rom.elf
input source-artifact:archive /path/to/linked-libphy.elf
input source-inventory:archive /path/to/libphy.a
input source-companion:rom /path/to/linked-libphy.elf
input source-companion:archive /path/to/esp32s31_rev0_rom.elf
input rust-artifact /path/to/rust-trace-probes.elf
