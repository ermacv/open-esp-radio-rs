schema = 4
id = "diagnostic-station-tx-egress-control-task-residence"
description = "Production-like station HT40 TX residence with affine egress control enabled"
repetitions = 3
image = "diagnostic-task-residence"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["diagnostic", "wifi", "station", "udp", "tx", "ht40", "split-core", "task-residence", "egress-control"]

[link]
phy = "ht40"
guard_interval = "any"

[workload]
kind = "udp"
direction = "tx"
duration_seconds = 12
tx_rate_bps = 130000000
payload_bytes = 1472

[criteria]
exact_delivery = false
require_no_beacon_loss = false
minimum_tx_bps = 90000000
maximum_idle_channel_utilization_255 = 64
