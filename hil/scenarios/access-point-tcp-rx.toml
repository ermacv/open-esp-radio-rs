schema = 4
id = "access-point-tcp-rx"
description = "Two AP ownership epochs with exact inbound TCP delivery"
image = "correctness"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["qualification", "wifi", "access-point", "network", "tcp", "rx"]

[workload]
kind = "access-point"
cycles = 2
boots = 1
timeout_seconds = 90

[workload.traffic]
kind = "tcp"
direction = "rx"
duration_seconds = 10
rx_rate_bps = 5000000
chunk_bytes = 8192

[criteria]
minimum_rx_bps = 4000000
