schema = 5
id = "bluetooth-trouble-secure-gatt-hci-read-failure"
description = "Secure pairing and bonded traffic across cold restart, then an injected shutdown HCI read error must retain the old owners and failure without cold release, restart or SoC reset; cleanup removes the temporary Linux bond"
repetitions = 3
transfer = "identical-image"
tags = ["bluetooth", "peripheral", "gatt", "trouble", "security", "automated", "failure"]

[bluetooth]
kind = "secure-gatt"
shutdown = "hci-read-failure"
