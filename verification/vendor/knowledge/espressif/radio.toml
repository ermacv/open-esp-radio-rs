# Reusable ESP-IDF/OSI vocabulary shared by radio investigations. This pack
# must not contain chip addresses, table slots, compiled providers or product
# verification policy.
schema = 3
id = "espressif-esp-idf-radio-v1"
knowledge-packs = [
    "../../../../tools/blobray/catalogs/neutral-embedded.toml",
    "esp-idf.toml",
]
capability-packs = ["capabilities.toml"]
interface-template-packs = ["interface-templates.toml"]
