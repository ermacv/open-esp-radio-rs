# Vendor contracts

Architecture- and platform-neutral immutable contract types shared by
analysis backends and optional knowledge providers. This crate depends only on
serialization support and contains no instruction semantics, chip identities,
artifact paths or input-authentication policy.
