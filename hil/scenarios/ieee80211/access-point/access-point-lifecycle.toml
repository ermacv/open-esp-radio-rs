schema = 5
id = "access-point-lifecycle"
description = "Repeated cold-boot and ownership qualification for one WPA2 access point"
tags = ["qualification", "wifi", "access-point", "lifecycle"]

[wifi]
image = "correctness"

[wifi.workload]
kind = "access-point"
cycles = 3
boots = 5
timeout_seconds = 90

[wifi.workload.traffic]
kind = "none"
