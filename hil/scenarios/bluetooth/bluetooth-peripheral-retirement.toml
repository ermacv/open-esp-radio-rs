schema = 4
id = "bluetooth-peripheral-retirement"
description = "Two real BLE peripheral ACL/update/recovery cycles, then Reset, live runner handoff, timer/IRQ/HCI retirement, shared IRQ output release, last-client RF close and checked physical cold-owner return with all Host authorities closed"
image = "bluetooth-dtm"
isolation = "reset"
repetitions = 3
tags = ["bluetooth", "peripheral", "acl", "lifecycle", "retirement"]

[workload]
kind = "bluetooth-peripheral"
boots = 1
connections = 2
hold_millis = 100
termination = "peer-reset"
retire_after = true
