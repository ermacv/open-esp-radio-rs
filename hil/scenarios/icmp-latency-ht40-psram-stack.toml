schema = 3
id = "icmp-latency-ht40-psram-stack"
description = "Experimental split-core HT40 station ICMP latency with ordinary task stacks in PSRAM"
repetitions = 3
image = "diagnostic-psram-stack"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["diagnostic", "icmp", "station", "ht40", "split-core", "psram-stack"]

[link]
phy = "ht40"

[workload]
kind = "icmp"
count = 100
interval_ms = 20
timeout_ms = 1000
payload_bytes = 56

[criteria]
maximum_lost = 0
maximum_p95_ms = 20
require_no_beacon_loss = true
