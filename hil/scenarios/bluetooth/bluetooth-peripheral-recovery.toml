schema = 4
id = "bluetooth-peripheral-recovery"
description = "Two real LE peripheral connections with exact Feature and Version Exchange, backpressured 251-byte bidirectional ACL fragmentation before and after exact Connection and Channel Map Updates, peer Reset disconnection and advertising recovery"
image = "bluetooth-dtm"
isolation = "reset"
repetitions = 3
tags = ["bluetooth", "peripheral", "connection", "feature-exchange", "version-exchange", "connection-update", "channel-map-update", "acl", "recovery"]

[workload]
kind = "bluetooth-peripheral"
boots = 1
connections = 2
hold_millis = 100
termination = "peer-reset"
