schema = 5
id = "bluetooth-peripheral-soak"
description = "One hundred sequential real LE peripheral connections with exact Feature and Version Exchange, bounded ACL backpressure, two bidirectional fragmented exchanges separated by exact Connection and Channel Map Updates, peer Reset, connection retirement and handle reuse"
repetitions = 1
tags = ["bluetooth", "peripheral", "connection", "feature-exchange", "version-exchange", "connection-update", "channel-map-update", "acl", "recovery", "soak"]

[bluetooth]
kind = "peripheral"
boots = 1
connections = 100
hold_millis = 100
termination = "peer-reset"
