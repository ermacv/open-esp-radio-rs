schema = 4
id = "timebase"
description = "Alarm and monotonic-clock agreement before radio initialization"
image = "correctness"
isolation = "reset"
data_plane = "split-radio-network"
tags = ["qualification", "timebase"]

[workload]
kind = "timebase"
boots = 3
intervals = 20
period_millis = 100

[criteria]
