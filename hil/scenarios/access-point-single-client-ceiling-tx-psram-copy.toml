schema = 3
id = "access-point-single-client-ceiling-tx-psram-copy"
description = "Same-image single-client AP HT40 UDP TX PSRAM-to-DMA materialization cost discriminator"
repetitions = 1
image = "diagnostic-tx-architecture"
isolation = "reset"
data_plane = "split-radio-network"
tx_buffer = "psram-staging-copy-diagnostic"
tags = ["diagnostic", "wifi", "access-point", "single-client", "ht40", "split-core", "tx", "psram", "copy", "task-residence"]

[link]
phy = "ht40"

[workload]
kind = "access-point"
cycles = 2
boots = 1
timeout_seconds = 120
client = "open-wrt"

[workload.traffic]
kind = "udp"
direction = "tx"
duration_seconds = 16
tx_rate_bps = 130000000
payload_bytes = 1472

[criteria]
exact_delivery = false
minimum_tx_bps = 1
minimum_concurrent_ap_clients = 1
