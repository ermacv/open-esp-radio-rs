[package]
name = "open-esp-radio-wifi-datapath"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Radio-native packet ownership and materialization contracts"

[dependencies]
open-esp-radio-dma = { path = "../../memory" }
open-esp-radio-network = { path = "../../network/interface" }

[features]
default = []
ownership-telemetry = []

[lints]
workspace = true
