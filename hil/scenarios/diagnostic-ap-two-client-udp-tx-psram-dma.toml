schema = 3
id = "diagnostic-ap-two-client-udp-tx-psram-dma"
description = "Two-client saturated Wi-Fi TX DMA experiment with descriptors in SRAM and scheduled packet buffers in PSRAM"
repetitions = 1
image = "diagnostic-task-residence"
isolation = "reset"
data_plane = "split-radio-network"
tx_buffer = "psram-direct-dma-diagnostic"
tags = ["diagnostic", "wifi", "access-point", "multi-client", "udp", "tx", "psram", "dma", "task-residence"]

[link]
phy = "ht40"

[workload]
kind = "access-point"
cycles = 2
boots = 1
timeout_seconds = 150
client = "laptop"

[workload.traffic]
kind = "udp-multi-client"
direction = "tx"
duration_seconds = 16
tx_rate_bps_per_flow = 65000000
payload_bytes = 1472

[criteria]
exact_delivery = false
minimum_tx_bps = 1
minimum_bps_per_flow = 1
maximum_flow_skew_percent = 100
minimum_concurrent_ap_clients = 2
