schema = 5
id = "bluetooth-dtm-maintenance-deadline"
description = "Active DTM TX/RX reaches autonomous PHY hard expiry, produces a new software-reset boot and RF silence without Host Test End or Reset"
repetitions = 3
transfer = "identical-image"
tags = ["bluetooth", "dtm", "phy-maintenance", "fail-stop"]

[bluetooth]
kind = "maintenance-deadline"
